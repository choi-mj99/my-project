# 공유 URL 검증

결과 화면까지 10문제를 자동 진행한 뒤 `결과 공유하기` 버튼을 눌렀다. 결과 화면 제목은 `일상 고통의 생활인`으로 정상 표시되었고 버튼도 정상 노출되었다. 공유 함수는 `window.location.href`를 결과 텍스트에 포함하도록 수정되어 현재 접속 중인 사이트 주소와 A/B 비율, 성향 분석명이 함께 클립보드에 기록된다.
